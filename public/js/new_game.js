let newGameFriendBut = document.getElementById("#new_game_friend_but");
let newGameComputerBut = document.getElementById("#new_game_computer_but");

newGameComputerBut.addEventListener(() => {
    newGame(true);
});

newGameComputerBut.addEventListener(() => {
    newGame(false);
});

function newGame(computer) {
    if (computer) {

    } else {

    }

    //TODO
    //window.location.href = "";
}